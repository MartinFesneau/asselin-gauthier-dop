<div class="container">
  <div class="row">
    <div class="col-12 col-lg-12 col-md-12">
      <div class="arrow-back">
        <%= link_to music_videos_path do%>
            <i class="fas fa-arrow-left"></i>
        <% end %>
      </div>
      <div class="content text-center">
        <h1><%= @project.artist %> / <%= @project.name %></h1>
        <h3>Réalisé par <%= @project.director %></h3>
        <h3>Produit par <%= @project.producer %></h3>

        <div class="embed-responsive embed-responsive-<%= @project.format %> mb-4">
          <iframe src="<%= @project.url %>" allowfullscreen></iframe>
        </div>
      </div>      
    </div>
  </div>
  <div class="row">
    <% @project.photos.each do |photo| %>
      <div class="col-12">
        <div class="screenshot">
        <%= cl_image_tag photo.key, alt: @project.name + " " + @project.artist %>
        </div>
      </div>
    <% end %>
  </div>
</div>
<%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>