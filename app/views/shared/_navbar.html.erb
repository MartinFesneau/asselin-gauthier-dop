<div class="mb-3 navbar navbar-expand-sm navbar-light navbar-lewagon <%= params[:action] == 'index' || params[:action] == 'contact' ? "transparent" : "normal" %>">
  <div class="container-fluid">
    <div class="title-cont">
      <%= link_to root_path, class:"active" do  %>
        <h1 class="title">ASSELIN GAUTHIER</h1>
      <% end %>
      <p class="subtitle">DIRECTOR OF PHOTOGRAPHY / CHEF OPERATEUR</p>
    </div>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="fas fa-bars navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <% if user_signed_in? && current_user.admin == true %>
            <%= link_to "ADD A PROJECT", new_project_path %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to "HOME", root_path  %>
        </li>
        <li class="nav-item">
          <%= link_to "PROJETS", root_path(anchor: "anchor") %>
        </li>
        <li class="nav-item">
          <%= link_to "CONTACT", contact_path %>
        </li>
        <li class="nav-item">
          <%= link_to "CV", cv_path %>
        </li>
      </ul>
    </div>
  </div>
</div>